# Version 2.0.6 Production Deployment Work Report

Date: 2026-08-08

Repository: `HKJoker-Z/personal-job-agent`

Status: **RELEASED**. Safe production-host capacity maintenance completed,
the already published immutable Version 2.0.6 artifacts were deployed, and
bounded post-release health and functional smoke passed. Production is
healthy on Version 2.0.6. No release object was recreated and no immutable tag
was moved.

## 1. Scope and previous STOPPED report

This report resumes the previously stopped deployment documented in
[`2026-08-08-v2.0.6-release-work-report.md`](2026-08-08-v2.0.6-release-work-report.md).
That phase had already verified the v2.0.6 tag, GitHub Release, official
images, release source, and rollback preparation, but stopped before any
production mutation because root capacity was approximately 5.02 GiB.

The approved release-preparation evidence was
[`2026-08-08-v2.0.6-release-preparation-work-report.md`](2026-08-08-v2.0.6-release-preparation-work-report.md),
with the immediately preceding production-candidate reports used as
supporting evidence. Repository and production state at execution time was
authoritative.

## 2. Starting production state

- Starting public Version: `2.0.5`.
- Starting root free capacity: `5,380,251,648` bytes, approximately `5.011`
  GiB, at the first read-only measurement.
- Starting Alembic current/head: `20260730_07`.
- Starting JD normalization mode: `java`.
- Starting Java policy: `jd-normalization-v1`.
- Starting skill dictionary: `skills-v1`.
- Starting production candidate source label:
  `7b834dd469892d2798661dca14f2f906e7b339cf`.
- Starting Backend/Worker/Outbox digest:
  `sha256:6bf10ee441ff50db693dfec31e6c2cdfac353d3e3bf62be59733aeb210adb1fa`.
- Starting Frontend/Edge digest:
  `sha256:70df317280ad5acd5e2916a0de65844b1add1bb54636cdeec1f793c8c93b174b`.
- Starting Java digest:
  `sha256:57e3e68c96ca629e4216e4cb19d55c0d9a52ad9bfb2d49c289fdc94f61f0d47f`.
- Starting PostgreSQL digest:
  `sha256:7c688148e5e1560d86df7ba8ae5a05a2386aaec1e2ad8e6d11bdf10504b1fb7`.
- Starting Redis digest:
  `sha256:c1e88455c85225310bbea54816e9c3f4b5295815e6dbf80c34d40afc6df28275`.

The bounded baseline was healthy, public HTTPS and `/api/health` returned
success, and no production content was inspected.

## 3. Disk investigation and classification

The read-only inventory found the following major root consumers:

| Consumer | Observed size or state | Classification | Action |
|---|---:|---|---|
| `/var/lib/containerd` | approximately 13 GiB | Managed image layers; includes active and candidate artifacts | No direct filesystem deletion |
| Docker images | 311 total, 8 active, 12.05 GB logical; 6.452 GB reclaimable | Active, rollback, official, candidate, and validation images | No broad image prune |
| Docker build cache | 177 entries, 2.87 GB total, 1.16 GB private reclaimable | Disposable build cache | Bounded builder cleanup |
| `/var/log/journal` | approximately 2.5 GB | System logs | Bounded journal retention |
| `/tmp` | approximately 3.3 GB | Mixed temporary validation/tooling output | Only named, verified validation caches removed |
| `/home/lighthouse/.local/share/Trash` | approximately 2.5 GB | Unknown user-owned storage | Preserved |
| `/var/cache/apt` | 365 MB | Regenerable package cache | Preserved after target was reached |
| Docker volumes | 9 total, 2 named production volumes | Production data and unrelated anonymous volumes | All preserved |

Protected or active objects were separated from disposable objects before any
cleanup. The two stopped production one-shot containers
`personal-job-agent-v2-migrate-1` and `personal-job-agent-v2-redis-init-1`
were retained. The stopped `job-agent-frontend-1` and separate running
`job-agent-backend-1` from the old `job-agent` project were classified as
unknown and retained. No stopped disposable validation container was removed.

The image inventory identified unreferenced `pja-v2.0.6-rc`, `pja-v2-final`,
JD normalization smoke, older v2.0.1, direct-network experiment, and other
validation images. They were individually distinguishable from active,
official, Java, PostgreSQL, Redis, and protected rollback images, but were
left intact because the capacity target was reached without image deletion.
No name-only image deletion was performed.

## 4. Protected objects

The following remained protected throughout maintenance and deployment:

- active production containers and writable data;
- `personal-job-agent-v2-postgres-data` and
  `personal-job-agent-v2-redis-data`;
- all other existing Docker volumes, including seven anonymous volumes;
- the published v2.0.6 tag and GitHub Release;
- official v2.0.6 application images;
- the unchanged Java image;
- PostgreSQL and Redis images;
- stable v2.0.5 rollback images;
- the protected rollback directory and restricted configuration files;
- production project knowledge, uploaded files, PostgreSQL data, Redis state,
  and source history.

The protected rollback snapshot is:

`/var/backups/personal-job-agent-v2/v2.0.5-pre-v2.0.6-candidate-20260807T020212Z`

Its bounded file-set hash after deployment was
`5479293cbbb026cd63c37f5200552334bcd1f468fcdbead5bfc1acd04a0fa989`.
The snapshot was not rewritten. The snapshot's original restricted
`production.env` and Java stage configuration remain available for exact
rollback; the live files were changed only as part of the versioned cutover.

## 5. Capacity maintenance

The following least-risk actions were completed:

1. `docker builder prune --force` removed only unused Docker builder cache and
   used no volume option. Docker builder total cache went from approximately
   `2.87 GB` to `1.117 GB`; host free-space increase measured by `df` was
   `960,638,976` bytes (`0.895` GiB).
2. Verified, unoccupied temporary release-validation resources were removed:
   `/tmp/pja-v206-trivy-cache`, `/tmp/pja-v206-trivy.itSlTw`,
   `/tmp/jd-phase1-toolchain`, `/tmp/job-agent-jdk21.dYS8WJ`,
   `/tmp/pja-v205-validation`, `/tmp/gh-cli-cache`, and
   `/tmp/node-compile-cache`. These were scanner caches and Java/Maven
   validation toolchains, not production data. Host free-space increase was
   `3,416,457,216` bytes (`3.182` GiB).
3. `journalctl --vacuum-size=512M` was used as the host's normal bounded log
   retention mechanism. It reported `2.0G` of archived journals freed, with
   journal usage reduced to approximately `464.1 MB`; host free-space increase
   was `2,227,449,856` bytes (`2.074` GiB). Recent active operational logs
   were retained.

No Docker image, stopped container, package cache, Docker volume, production
data file, rollback file, secret/configuration backup, or application log file
was manually deleted. The forbidden broad `docker system prune -a --volumes`
operation was not used.

After maintenance, before deployment, root free capacity was
`11,984,797,696` bytes, approximately `11.162` GiB. The final post-deployment
measurement was `11,984,613,376` bytes, approximately `11.162` GiB. The small
decrease was normal runtime activity. Total measured free-space increase from
the starting point to the final state was `6,604,361,728` bytes.

## 6. Preservation audit and rollback readiness

After cleanup and before deployment:

- public Version remained `2.0.5`;
- Backend, Frontend/Edge, Worker, Outbox, Java, PostgreSQL, and Redis were
  healthy;
- restart counts were zero and `OOMKilled` was false;
- public HTTPS and health endpoints succeeded;
- Alembic remained `20260730_07`;
- Java mode, policy, and dictionary remained `java`,
  `jd-normalization-v1`, and `skills-v1`;
- all nine Docker volumes still existed;
- the protected snapshot file set and permissions remained intact;
- all protected rollback image references remained immediately available.

Protected v2.0.5 rollback digests were verified as:

- Backend/Worker/Outbox:
  `sha256:79fac56ae0884cc5362356c2d3d3f981e681286e9214faea4b4a4c1d03255b57`;
- Frontend:
  `sha256:325bae0c95b8f571e6d1a5a64dff4ae3012ff71c929c15e7c47aebe4652c0996`;
- Edge:
  `sha256:09e80b4d51f1069458fe8c4a55ef3b2796789e1191fd9f8fa43c77288d45ebd9`;
- Java:
  `sha256:57e3e68c96ca629e4216e4cb19d55c0d9a52ad9bfb2d49c289fdc94f61f0d47f`.

Because the schema was unchanged, rollback remains an exact image/config
restoration and does not require an Alembic downgrade or any PostgreSQL/Redis
data operation.

## 7. Pre-deployment gate

The complete execution-time gate passed:

- root free capacity exceeded the operational `>=8 GiB` threshold and the
  preferred `>=10 GiB` target;
- `v2.0.6` remote tag still resolved to
  `f2d0a7d68a37d3d6c7c9303f219da80fb491a497`;
- the published GitHub Release remained non-draft and non-prerelease;
- official image registry inspection returned the expected v2.0.6 index
  digests;
- publication run [31240123357](https://github.com/HKJoker-Z/personal-job-agent/actions/runs/31240123357)
  and tag verification run [31240118497](https://github.com/HKJoker-Z/personal-job-agent/actions/runs/31240118497)
  were successful for the exact source SHA;
- production baseline, health, rollback assets, volumes, and config were
  known and preserved;
- no unexpected source, tag, Release, or production service change was found;
- no Provider benchmark or application validation cohort was run.

The immutable tag verification was:

```text
refs/tags/v2.0.6       e06bb8f5063735ad19a17ea9cc148cfc6a0b12cc (annotated object)
refs/tags/v2.0.6^{}    f2d0a7d68a37d3d6c7c9303f219da80fb491a497 (peeled commit)
```

## 8. Published release and image traceability

- Final release source SHA: `f2d0a7d68a37d3d6c7c9303f219da80fb491a497`.
- Release tag: `v2.0.6`.
- Tag target SHA: `f2d0a7d68a37d3d6c7c9303f219da80fb491a497`.
- GitHub Release: [Version 2.0.6](https://github.com/HKJoker-Z/personal-job-agent/releases/tag/v2.0.6).
- Official release workflow: [Release Container Images run 31240123357](https://github.com/HKJoker-Z/personal-job-agent/actions/runs/31240123357).
- Tag-to-source verification workflow: [run 31240118497](https://github.com/HKJoker-Z/personal-job-agent/actions/runs/31240118497).

| Shipped component | Official semantic/source-SHA tag | Immutable registry index digest | Source |
|---|---|---|---|
| Backend; Worker; Outbox | `ghcr.io/hkjoker-z/personal-job-agent-backend:sha-f2d0a7d68a37d3d6c7c9303f219da80fb491a497` | `sha256:3d11c32f60c0f409e13ebfdf278cadb372615cf815b2dd1fe76e100ec5262af4` | `f2d0a7d68a37d3d6c7c9303f219da80fb491a497` |
| Frontend; Edge | `ghcr.io/hkjoker-z/personal-job-agent-frontend:sha-f2d0a7d68a37d3d6c7c9303f219da80fb491a497` | `sha256:c0950016ec7d415ae9a80679149646972a3d514f44f348e72eb864933b340d09` | `f2d0a7d68a37d3d6c7c9303f219da80fb491a497` |
| Java normalization | Existing approved immutable image; not rebuilt | `sha256:57e3e68c96ca629e4216e4cb19d55c0d9a52ad9bfb2d49c289fdc94f61f0d47f` | unchanged approved image |

The official linux/amd64 manifest digests were
`sha256:288952fd6f01d52040bb94bfa5b4feda6a7047cd51a41501aa3d448ec870d913`
for Backend and
`sha256:f49cf19a823947bc7b9a39854da3628cb8135c0a2e68cfeb2e9dc68566cdeb21`
for Frontend. Production used the verified immutable registry index references
shown above. The traceability chain was exact:

`source SHA -> v2.0.6 tag -> official workflow -> immutable digest -> running container digest`.

## 9. Deployment sequence

Only the existing production host deployment configuration was changed:

- `compose.yaml` Backend and Frontend/Edge immutable anchors were changed to
  the official v2.0.6 digests and `APP_VERSION` was changed to `2.0.6`;
- the protected production environment's `APP_VERSION` was changed to `2.0.6`;
- the protected Java stage-2 environment's `BACKEND_IMAGE` was changed to the
  official Backend v2.0.6 digest so Worker/Outbox/Backend rendering could not
  resolve ambiguously to the old stage image;
- Compose configuration was rendered with the established override order and
  passed `config --quiet` before mutation;
- `worker` and `outbox-dispatcher` were recreated with `--no-build --pull never
  --no-deps`, then awaited healthy;
- `backend` was recreated with the same bounded options and awaited healthy;
- `frontend` and `edge` were recreated with the same bounded options and
  awaited healthy;
- no `migrate` command, Alembic upgrade/downgrade, Redis initialization, or
  database operation was run;
- Java, PostgreSQL, Redis, networks, named volumes, and persistent data were
  not recreated or altered.

## 10. Production state after deployment

- Public Version: `2.0.6`; public `/api/health`: HTTP 200.
- Public HTTPS root: HTTP 200.
- Backend/Worker/Outbox running digest:
  `sha256:3d11c32f60c0f409e13ebfdf278cadb372615cf815b2dd1fe76e100ec5262af4`.
- Frontend/Edge running digest:
  `sha256:c0950016ec7d415ae9a80679149646972a3d514f44f348e72eb864933b340d09`.
- Java running digest:
  `sha256:57e3e68c96ca629e4216e4cb19d55c0d9a52ad9bfb2d49c289fdc94f61f0d47f`.
- PostgreSQL running digest:
  `sha256:7c688148e5e1560d86df7ba8ae5a05a2386aaec1e2ad8e6d11bdf10504b1fb7`.
- Redis running digest:
  `sha256:c1e88455c85225310bbea54816e9c3f4b5295815e6dbf80c34d40afc6df28275`.
- Backend readiness: HTTP 200.
- Java readiness: `UP`.
- PostgreSQL `pg_isready`: passed.
- Redis `PING`: `PONG`.
- Alembic current/head: `20260730_07`.
- Backend environment: `APP_VERSION=2.0.6`, mode `java`, policy
  `jd-normalization-v1`, dictionary `skills-v1`.
- Worker, Outbox, Backend, Frontend, Edge, Java, PostgreSQL, and Redis were
  healthy in immediate and three-sample stability checks.
- All inspected restart counts were `0`; all inspected `OOMKilled` states were
  `false`.
- No crash loop or image mismatch was observed.

## 11. Minimal functional smoke

The repository's existing bounded production-candidate collector was invoked
in a one-shot process with the current v2.0.6 immutable baseline. Its source
was not modified. It created one synthetic operator, performed exactly one
synthetic Analyze first request and one completed replay, retained only
bounded metadata, and deleted the synthetic user and associated rows.

Results:

- Supplemental execution count: `1`.
- Analyze result state: `complete`.
- Public JSON/HTTP success: passed.
- Synthetic Resume path: passed using the collector's generated DOCX fixture.
- Authentication/session access: passed.
- Job Summary: present-or-available (`true`).
- Match Reasons: present-or-available (`true`).
- Provider call count: `1`, within the configured maximum of `3`.
- Analyze duration: approximately `7,801.322 ms`, inside the authoritative
  deadline (`true`).
- Security defect: `false`.
- Serialization defect: `false`.
- History finalization: `true`, exactly one synthetic History record observed.
- The completed idempotency replay was recognized and returned the same result.
- Replay Provider-call delta: `0`.
- Same History record: `true`.
- Duplicate History: `false`.
- Cleanup: passed; remaining synthetic users, application records, analysis
  metrics, step metrics, and idempotency rows were all `0`.

The smoke result was complete; fallback was not selected in this single
execution. A fallback or partial result would have remained a supported
non-rollback state if all bounded correctness gates had passed.

## 12. Security, serialization, and persistence result

Authentication and the Origin/CSRF-bearing Analyze/logout path passed without
secret or content leakage. The bounded collector reported no security defect
and no serialization defect. No request/response body, Resume, JD, Provider
body, prompt, reasoning, cookie, authorization value, credential, or complete
environment file was recorded or printed.

History finalization and completed idempotency replay passed with exactly-once
synthetic persistence and zero replay Provider calls. No production user data
was inspected.

## 13. Deferred findings and release scope

- Controlled Provider quality remains the carried-forward **DEGRADED**
  observation at `7/10` accepted. It was not rerun or improved in this phase;
  Provider acceptance percentage remains separate from hard correctness gates.
- React Router is locked at `7.18.2`; the production dependency audit remains
  clean for the shipped graph. Three high transitive development-chain
  findings (`nanoid`, `postcss`, and `undici`) remain deferred.
- The preparation report's no-fixed-version Debian base-image findings remain
  deferred maintenance work.
- The remaining host capacity risk is future growth in managed containerd
  layers, unreferenced candidate images, journal/cache growth, and unknown
  user-owned Trash. Free root capacity is currently approximately `11.162`
  GiB; capacity expansion and a reviewed retention policy remain sensible
  follow-up infrastructure work.
- PR #57 remains open as the future direct-network experiment with head
  `db1cfac86bc393ac85a85b424688aa126a42dd19`. It was not merged, cherry-picked,
  or deployed.

## 14. Rollback conditions and result

Hard rollback conditions remain: public Version or immutable digest mismatch,
unhealthy Backend/Frontend/Edge/Java/Worker/Outbox, authentication or
CSRF/Origin regression, Resume or History regression, security or secret
leakage, serialization failure, deadline or Provider-call-limit breach,
duplicate persistence, broken completed replay, Java regression,
PostgreSQL/schema mismatch, restart loop, OOM, or an untrustworthy rollback
path.

No rollback condition occurred. **Rollback did not occur.** Rollback readiness
is `yes`: the exact v2.0.5 image digests and protected configuration snapshot
remain available, and rollback requires no Alembic downgrade or data mutation.
The v2.0.6 tag and GitHub Release remain published and were not deleted or
moved.

## 15. Reporting delivery, commits, and checks

Changed repository files for this final reporting update:

- `docs/work-reports/2026-08-08-v2.0.6-production-deployment-work-report.md`;
- `docs/work-reports/README.md`.

Relevant commits and delivery:

- PR #61 normal merge commit/source SHA:
  `f2d0a7d68a37d3d6c7c9303f219da80fb491a497`;
- prior documentation branch head before this report:
  `ea4d93270b9c699d53f906e700c18f9502e1e4e3`;
- final report branch/PR: [PR #62 — Docs: Record Version 2.0.6 final release](https://github.com/HKJoker-Z/personal-job-agent/pull/62);
- this report is documentation-only and is not a prerequisite for the
  successful production deployment.

Final release and deployment checks were successful:

- merged-main CI: [31239861113](https://github.com/HKJoker-Z/personal-job-agent/actions/runs/31239861113);
- Java candidate CI: [31239861112](https://github.com/HKJoker-Z/personal-job-agent/actions/runs/31239861112);
- official image publication: [31240123357](https://github.com/HKJoker-Z/personal-job-agent/actions/runs/31240123357);
- tag/image verification: [31240118497](https://github.com/HKJoker-Z/personal-job-agent/actions/runs/31240118497).

## 16. Required confirmations and final decision

- No new ten-case Provider cohort was run.
- No Provider prompt, response schema, acceptance/salvage behavior, timeout,
  deadline, retry, call maximum, model, proxy, network, Mihomo, or
  `NO_PROXY` behavior was changed.
- Java logic, image, policy, dictionary, and production topology remained
  unchanged.
- No Alembic migration, upgrade, or downgrade occurred; current/head remained
  `20260730_07`.
- No production/user content was inspected.
- PR #57 was not merged.
- The immutable `v2.0.6` tag was not moved, force-updated, deleted, or
  recreated.
- Production rollback did not occur.

**RELEASED — production healthy on v2.0.6.**

Remaining follow-up items are documentation PR #62 review/merge, host capacity
retention or expansion planning, the deferred dependency/base-image findings,
and separate monitoring of Provider quality. No additional v2.0.6 release
action is required.
