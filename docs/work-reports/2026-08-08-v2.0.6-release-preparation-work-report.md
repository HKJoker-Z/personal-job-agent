# Version 2.0.6 Release Preparation Work Report

Date: 2026-08-08
Repository: `HKJoker-Z/personal-job-agent`
Status: release preparation is complete locally; Version 2.0.6 is not
published, tagged, or deployed.

## 1. Repository

Repository: `https://github.com/HKJoker-Z/personal-job-agent`.

The release branch is `release/v2.0.6`. The working tree was kept free of
tracked generated output and production configuration content.

## 2. PR #60 final head and merge commit

PR #60 (`Fix: Complete production candidate hard-gate evidence`) finished at
head `9699d328d19c9cb6c6be91f0edbaf5af897de61f`. It was merged with the normal
merge commit
`ec2eb83febc9fe6d9322ed56e9f4b84c5f05c38b`, preserving both parents. It was
not squashed, rebased, or admin-bypassed.

The merged correction covered the collector correction, collector tests, and
the two 2026-08-07 evidence reports. The required PR checks were successful,
including backend, PostgreSQL, frontend, Compose, Docker smoke, repository
safety, Java verify, Java container smoke, and Java normalization-only checks.

## 3. Starting main SHA

After the PR #60 merge, `main` was checked out and updated with
`git pull --ff-only origin main`. Local `main` equaled `origin/main` at
`ec2eb83febc9fe6d9322ed56e9f4b84c5f05c38b`. Merged-main CI run
`31234319716` completed successfully.

## 4. Release branch

`release/v2.0.6` was created from that exact updated `main`, not from the old
candidate branch. The branch initially pointed at
`4402db0265a28f7cdd2bc6fb52926ca394360d9b` and the source SHA used for the
release-candidate images is
`a9b2ab1cd678f310456832c7edc6b2beafd27ee5`.

The final branch SHA and release-preparation PR metadata are recorded in the
final update to this report after the PR is opened.

## 5. Production baseline

- Public production version: `v2.0.5`.
- Production-candidate source revision:
  `7b834dd469892d2798661dca14f2f906e7b339cf`.
- Alembic current/head: `20260730_07`.
- JD normalization mode: `java`.
- Java policy: `jd-normalization-v1`.
- Skill dictionary: `skills-v1`.
- `/api/analyze`: synchronous.
- Version 2.0.6: unreleased during this work.

## 6. Production-candidate evidence carried forward

The authorized synthetic production candidate recorded 10/10 Analyze
executions with HTTP 200 public JSON for 10/10. Result states were complete
6/10, repaired 0/10, partial 1/10, and fallback 3/10. Provider acceptance
was 7/10 and Provider quality was **DEGRADED**. The maximum Provider-call
count was 2, deadline exhaustion was 0, and maximum Analyze duration was
approximately 8.276 seconds. Security defects and serialization failures
were both 0. History and idempotency finalized 10/10, with no duplicate
History observed.

Backend, Frontend/Edge, Worker, Outbox, Java, PostgreSQL, and Redis were
healthy in the recorded production evidence, with zero relevant restart loops
and no OOM condition.

Provider acceptance percentage was treated as a monitoring observation, not a
release hard gate. Deterministic fallback is a supported product state.

## 7. Supplemental hard-gate evidence

A separate deterministic first-request/replay check recorded:

- first request: HTTP 200, state `fallback`, two Provider calls;
- Job Summary and Match Reasons: present or explicitly unavailable;
- History finalized exactly once and idempotency finalized;
- completed replay recognized, same History record reused, duplicate History
  false;
- replay Provider-call delta: 0;
- replay duration: approximately 17.876 ms;
- security defect: false; serialization defect: false.

## 8. Old version

Old public version: `2.0.5`.

## 9. New version

Prepared version: `2.0.6`.

## 10. Canonical version files changed

The authoritative version locations and repository-required derived metadata
were updated from `2.0.5` to `2.0.6` in:

- `.env.production.example`;
- `backend/app/__init__.py` and `backend/config.py`;
- `backend/Dockerfile` and `frontend/Dockerfile` image defaults;
- `frontend/package.json` and `frontend/package-lock.json` root metadata;
- `compose.yaml`, `compose.postgres16-restore.yaml`, and
  `ops/candidate/java-normalization/compose.yaml`;
- `scripts/v2_backup_restore.py`;
- the required CI and image workflow metadata in
  `.github/workflows/backend-production.yml`, `.github/workflows/ci.yml`,
  and `.github/workflows/release-images.yml`;
- expected public version assertions in
  `backend/test_data_management_service.py` and `backend/test_readiness.py`;
- the 2.0.6 scope in `scripts/docker-smoke-v2.sh`.

No additional invented version file was added.

## 11. Release scope

The release scope is the already merged bounded Provider/degraded-output
resilience work, deterministic fallback and field salvage behavior, stable
completion fields, monitoring categorization, candidate collector hardening,
the directly used `pypdf` security patch, the narrowly scoped frontend
advisory correction, source/version metadata, and evidence-grounded
documentation.

## 12. Provider behavior shipped

The source carries the merged behavior for one bounded absolute Provider
deadline across primary/retry/repair/body/finalization work, pragmatic shallow
Provider acceptance, backend-authoritative scoring and evidence, deterministic
skills, field-level salvage into `partial`, stable Job Summary and Match
Reasons completion behavior, bounded error categorization, and safe
deterministic `fallback` completion. `/api/analyze` remains synchronous.

## 13. Provider behavior explicitly not shipped

PR #57's dedicated direct-network implementation was not merged and is not
represented as shipped. This preparation does not ship `trust_env=False`,
`DEEPSEEK_NETWORK_MODE`, direct-network routing, a new DeepSeek prompt,
acceptance, timeout, retry, or networking experiment, or asynchronous/queued
Analyze behavior. It does not claim zero Provider failures, fully healthy
Provider quality, or 100% Provider acceptance.

## 14. Java status

Java source is unchanged relative to updated `main`. Production remains a
private/internal normalization-only service with no host public port, no
PostgreSQL or Redis runtime dependency, and the unchanged normalization
endpoint contract. Policy remains `jd-normalization-v1`, dictionary remains
`skills-v1`, and the Request ID/hash/version contract and `fallback_local`
behavior are unchanged. No Java Resume PDF/DOCX extraction was added.

## 15. Alembic status

Alembic current and head are both `20260730_07`. The release source adds no
migration, requires no upgrade, and requires no downgrade. No empty migration
was generated.

## 16. PROJECT_KNOWLEDGE changes

`docs/PROJECT_KNOWLEDGE.md` was refreshed only for merged behavior. It records
FastAPI orchestration, Java JD normalization, Provider deadlines and
pragmatic acceptance, deterministic scoring, RAG, security, idempotency,
monitoring, fallback behavior, and production validation. It corrects the
current Alembic mapping to `20260730_07` and does not include the cancelled
Java Resume Extraction concept or PR #57 networking.

## 17. README and documentation changes

`README.md` now reflects Version 2.0.6 release-candidate history and the
Version 2.0.5 immutable rollback baseline. `docs/DEPLOYMENT.md` reflects the
current pre-release production baseline. `docs/V2_0_6_RELEASE_NOTES.md`
contains the release notes and explicit non-shipped scope. The Work Report
index was updated with this report. Existing historical rollout documents
were not rewritten as if their historical versions were current.

Public Analyze result-state terminology remains exactly `complete`,
`repaired`, `partial`, and `fallback`. `completed_with_warnings` remains a
monitoring term, not a fifth user result state.

## 18. React Router advisory investigation

The previously locked frontend graph used `react-router-dom` 7.18.1. The
recorded advisory was `GHSA-qwww-vcr4-c8h2`, concerning unstable React Router
RSC APIs in the affected range. The application is a client-only Vite SPA and
does not use the RSC APIs. The actual locked graph was inspected after
`npm ci --ignore-scripts`.

## 19. Advisory remediation/defer decision

Decision: **B**. The smallest compatible locked update was applied:
`react-router-dom` and its locked `react-router` package changed from 7.18.1
to 7.18.2. No downgrade and no unrelated dependency upgrade was performed.
`npm audit --omit=dev --json` reports 0 vulnerabilities in the shipped
production dependency graph. The complete audit still reports three high
transitive development-chain findings in `nanoid`, `postcss`, and `undici`
through Vite/jsdom; they are outside the shipped runtime and are deferred as
a separate dev-tool follow-up.

## 20. Backend test result

With inherited SOCKS proxy variables removed from the local test environment,
the complete backend unit suite passed:

`Ran 555 tests in 244.299s`
`OK (skipped=12)`

The suite covered Provider deadline, pragmatic acceptance, security,
idempotency, History, monitoring, serialization, fallback, and Analyze
contracts. The initial proxy-related local error was environmental; the
proxy-free rerun passed. No real DeepSeek request was made.

## 21. PostgreSQL integration result

The isolated PostgreSQL 16 integration suite passed with zero skipped required
tests:

`Ran 12 tests in 41.452s`
`OK`

It used a disposable PostgreSQL 16.9 test container and synthetic test data,
not the production database.

## 22. Frontend test/build result

- `npm ci --ignore-scripts`: passed.
- `npm test`: 9 test files passed, 70 tests passed.
- `npm run build`: Vite production build passed.
- production dependency audit: 0 vulnerabilities.

## 23. Java Maven and smoke result

`./mvnw -B verify` passed with 46 tests, 0 failures, 0 errors, and 0 skips.
The normalization-only container smoke passed with image manifest
`sha256:5823b6665da640928790f08553ec6371ccce3eb582440b3727eeddb5b1f89aa2`,
healthy state, zero restarts, no OOM, no database container, and no public
normalization port. The full-profile container smoke passed with application
manifest `sha256:7509d6fe2285248b22e313bc852f06dbc24972885d7d82d8e4bd80910a6cd317`
and migration manifest
`sha256:e4cbcc2c58d7644f21f5539bb0b531ebc00bc8bf71468646e641a32249503c7c`.
Migration, replay, update, History, persistence, application health, and
PostgreSQL health checks passed with zero restart/OOM conditions.

These Java smoke images were disposable validation artifacts; the unchanged
immutable production Java image may be reused for release deployment.

## 24. Compose validation

Production `deploy/production/compose.yaml` and root `compose.yaml` both
passed `docker compose config --quiet` with synthetic validation variables.
The isolated production-runtime regression passed, including stage-2 local,
stage-3 shadow, stage-4 Java topology assertions, private network aliases,
Nginx upstream names, read-only edge constraints, and Redis initialization
checks. The full isolated Version 2.0.6 Docker smoke also passed with mock
Provider behavior and cleaned its temporary project.

## 25. Dependency and vulnerability scan

- `pip check`: passed.
- `npm audit --omit=dev`: 0 vulnerabilities.
- Full npm audit: 3 high transitive dev-only findings (`nanoid`, `postcss`,
  `undici`); no critical production dependency finding. They are documented
  and deferred rather than hidden or downgraded.
- Trivy 0.65.0, pinned for this scan by digest
  `sha256:a22415a38938a56c379387a8163fcb0ce38b10ace73e593475d3658d578b2436`,
  reported 0 fixable HIGH/CRITICAL findings with `--ignore-unfixed` for both
  release-candidate images. The frontend also reported 0 HIGH/CRITICAL
  findings without that filter.
- The unfiltered Backend scan reported 55 Debian package records, including
  affected/fix-deferred records in Perl, util-linux, curl, ncurses, gzip, and
  related base packages. These have no fixed version in the current feed and
  are not used by the application execution path; PostgreSQL client-common
  brings Perl as a package dependency. This is recorded as a base-image
  maintenance follow-up, not silently ignored. No applicable fixed critical
  application dependency remained for this release gate.

## 26. Secret scan

Repository safety checks passed: no tracked runtime data, environment secret
file, database, build output, or Java target output was added; ShellCheck and
`git diff --check` passed. The only credential-pattern matches in tracked
content were explicitly allowlisted synthetic fixtures/examples used by the
repository's own security tests. `scripts/verify-images.sh` passed for both
candidate images, Docker history checks found no secret/API-key material, and
Trivy image secret scanning found no issue in either image.

## 27. Image tags

- Backend/Worker/Outbox shared candidate tag:
  `pja-v2.0.6-rc-backend:sha-a9b2ab1`.
- Frontend/Edge shared candidate tag:
  `pja-v2.0.6-rc-frontend:sha-a9b2ab1`.
- Java is unchanged and references the existing immutable production image;
  no new public Java image was required.

## 28. Image digests

- Backend/Worker/Outbox shared digest:
  `sha256:f3ae959cf45688c1868c398b4b4466855cf5b0db4146d317985eb7dcd248a3d5`.
- Frontend/Edge shared digest:
  `sha256:dd049b1b53831a5b54c789e9c513becc4cb6e4629e90b18bc85a42ac17bd39c4`.

These are local immutable candidate image digests. They were not pushed to a
registry and are not public release tags.

## 29. Source SHA for images

Both release-candidate images were built from exact source SHA
`a9b2ab1cd678f310456832c7edc6b2beafd27ee5`, with OCI revision labels set to
that SHA and `org.opencontainers.image.version=2.0.6`. The build label
timestamp was `2026-08-08T03:50:41Z`.

## 30. Release notes summary

The release notes describe bounded Provider deadlines, safer graceful
fallback, pragmatic shallow output acceptance, backend-authoritative score
and evidence, smaller Provider responsibility, field-level salvage, stable
Job Summary/Match Reasons, idempotent replay correctness, monitoring
categorization, the PDF parser patch, operational collector hardening, and
the carried-forward hard-gate evidence. They explicitly describe Provider
quality as **DEGRADED** at 7/10 accepted and do not market it as success.

## 31. Version 2.0.5 rollback digests

The recorded immutable rollback assets are:

- Backend: `sha256:79fac56ae0884cc5362356c2d3d3f981e681286e9214faea4b4a4c1d03255b57`.
- Worker/Outbox: the same shared Backend digest above.
- Frontend: `sha256:325bae0c95b8f571e6d1a5a64dff4ae3012ff71c929c15e7c47aebe4652c0996`.
- Edge: `sha256:09e80b4d51f1069458fe8c4a55ef3b2796789e1191fd9f8fa43c77288d45ebd9`.
- Java normalization: `sha256:57e3e68c96ca629e4216e4cb19d55c0d9a52ad9bfb2d49c289fdc94f61f0d47f`.

## 32. Rollback procedure

1. Stop accepting release traffic and restore the reviewed Version 2.0.5
   immutable Compose/configuration revision.
2. Restore the Backend, Worker, Outbox, Frontend, Edge, and Java image
   references to the exact digests in section 31.
3. Preserve PostgreSQL and Redis volumes. Verify that PostgreSQL schema remains
   compatible at `20260730_07`; no Alembic downgrade is required.
4. Verify public version `2.0.5`, readiness, health, restart count, OOM state,
   Java private topology, and edge upstreams before reopening traffic.

Rollback is an immutable image/config restoration and does not modify
production during this preparation.

## 33. Changed files

The release branch changes are the version/build metadata, backend security
dependency pin, frontend advisory lock update, release documentation, and
validation/operation metadata in:

`.env.production.example`, `.github/workflows/backend-production.yml`,
`.github/workflows/ci.yml`, `.github/workflows/release-images.yml`,
`README.md`, `backend/Dockerfile`, `backend/app/__init__.py`,
`backend/config.py`, `backend/requirements.txt`,
`backend/test_data_management_service.py`, `backend/test_readiness.py`,
`compose.postgres16-restore.yaml`, `compose.yaml`, `docs/ARCHITECTURE.md`,
`docs/CI_CD.md`, `docs/DEPLOYMENT.md`, `docs/PROJECT_KNOWLEDGE.md`,
`docs/V2_0_6_RELEASE_NOTES.md`, `docs/V2_DEVELOPMENT.md`,
`docs/V2_PRODUCTION_READINESS.md`, `docs/V2_ROADMAP.md`,
`docs/adr/0001-modular-monolith.md`, `docs/adr/README.md`,
`docs/architecture/JAVA_PRODUCTION_NORMALIZATION_INTEGRATION.md`,
`frontend/Dockerfile`, `frontend/package-lock.json`, `frontend/package.json`,
`frontend/src/components/AppLayout.jsx`, `frontend/src/legacy-workspace.jsx`,
`frontend/src/pages/ArchitecturePage.jsx`, `frontend/src/pages/FeatureStatePage.jsx`,
`ops/candidate/java-normalization/compose.yaml`,
`scripts/docker-smoke-v2.sh`, `scripts/postgres16-restore-regression.sh`,
`scripts/v2_backup_restore.py`, and this report plus
`docs/work-reports/README.md`.

## 34. Commits

The release-preparation branch commits before this report are:

- `300093992cd54086c27799f5ff7006025924626c` — prepare: define Version 2.0.6 release candidate;
- `861a79859befc4d531ffe1094e7cb262b0299562` — fix: patch PDF parser security advisory;
- `4402db0265a28f7cdd2bc6fb52926ca394360d9b` — test: align Version 2.0.6 smoke scope;
- `a9b2ab1cd678f310456832c7edc6b2beafd27ee5` — docs: correct v2.0.6 release baseline references.

The report/index commit and subsequent release-PR metadata commit will be
added here before the final decision.

## 35. Release-preparation PR URL

Pending creation from `release/v2.0.6` with title
`Release: Prepare Version 2.0.6`. This section is updated after the PR is
opened; it is not merged automatically in this phase.

## 36. GitHub checks

PR #60 and merged-main CI passed as described above. The release-preparation
PR checks are pending creation and will be waited on to completion. The final
report update will record the PR URL, check conclusions, and
`CLEAN`/`MERGEABLE` state.

## 37. GO / NO-GO

Provisional decision: **GO for release-preparation PR review and publishing
gate**, subject to all final release-preparation PR checks passing. This is
not authorization to publish, tag, create a GitHub Release, or deploy in this
phase.

## 38. Exact next prerequisite

Open `Release: Prepare Version 2.0.6` from `release/v2.0.6`, wait for every
required GitHub check, confirm the PR is `CLEAN` and `MERGEABLE`, and obtain
the normal review/merge needed before entering the separate publishing phase.

## 39. Known risks

- Provider quality remains a monitored **DEGRADED** observation at 7/10
  accepted in the carried-forward candidate.
- Real Provider behavior can still produce bounded fallback; this is
  supported and not a user error state.
- The three high frontend findings are dev-only transitive toolchain findings
  and require a separate compatible toolchain follow-up.
- The unfiltered Backend base-image scan has no-fixed-version Debian package
  findings documented in section 25; a base-image maintenance follow-up is
  required, but no applicable fixed critical application dependency was found.
- PR #57's networking experiment remains outside this release.
- Registry publication, GitHub Release creation, production deployment, and
  final tag creation have not occurred.

## 40. Provider quality note

Provider acceptance was 7/10 and Provider quality was **DEGRADED** in the
controlled candidate. The product hard gates passed on public correctness,
bounded execution, security, serialization, History, and idempotency. Quality
is monitored separately from those hard gates. It is not represented as a
success metric, and no claim of 100% acceptance is made.

## 41. No new real-Provider cohort

No new real-Provider ten-case cohort was run. Automated validation used only
Mock Provider behavior and synthetic fixtures. The prior production-candidate
evidence was carried forward as directed.

## 42. Production untouched

No production container, service, database, Redis instance, deployment file,
secret, or user data was mutated during this preparation. All new containers
and databases were disposable isolated validation resources and were cleaned
up.

## 43. No production/user content inspected

No production or user Resume, JD, Provider body, prompt, database row,
Project Knowledge content, credential, or production log was inspected. Tests
used only synthetic fixtures and the repository's sanitized examples.

## 44. No Alembic migration occurred

No Alembic migration file was added, removed, generated, upgraded, or
downgraded for Version 2.0.6. Current/head remain `20260730_07`.

## 45. PR #57 was not merged

PR #57 remains open with head
`db1cfac86bc393ac85a85b424688aa126a42dd19` and currently has a conflicting
merge state. It was not modified, cherry-picked, or merged. Recommendation:
leave it open as a future networking experiment and preserve its Work Reports;
do not represent its direct-network code as shipped.

## 46. No Version 2.0.6 tag or GitHub Release

No `v2.0.6` tag was created, no GitHub Release was created, and no production
deployment was performed. These actions belong to the next publishing phase
after the release-preparation PR is normally reviewed and merged.
