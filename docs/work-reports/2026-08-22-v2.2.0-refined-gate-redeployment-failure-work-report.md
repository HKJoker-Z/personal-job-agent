# v2.2.0 Refined-Gate Redeployment 失败与回滚报告

## 结论

- 本次使用新 release gate 重新执行 v2.2.0 完整发布，最终为 **NO-GO / HARD FAIL / ROLLED BACK**。
- Candidate 完整功能 smoke 与 5 次 production-equivalent Analyze 均通过；Candidate verdict 为 **PASS**，5/5 HTTP 200、Java fallback 0/5。
- Production cutover 完成后，正式功能验收的第一个 Public HTTPS Analyze 出现 `Empty reply`。虽然 Java 和 Backend 都完成并记录 HTTP 200，但客户端没有收到 HTTP status 或 response bytes，因此严格按 HARD FAIL 保留证据并立即回滚。
- 当前 Production 已恢复为健康的 v2.1.0，Alembic 仍为 `20260820_08`。没有绕过 hard gate，也没有创建成功发布报告。

## Release gate policy 与实现

- Ops branch：`ops/refine-release-gates`
- Gate commit：`bba68e04e12ad077c8824fe291a896d48285a21c`（已 push，并 fast-forward 合入 `main`）
- GitHub Actions run：`32571910233`，全部 jobs 通过，包括新增 `release-gate-tests`。
- 新实现：`ops/release_gate/analyze_gate.py`、`collect_analyze.py`、production-length synthetic Resume/JD fixtures、8 个 gate tests；`docs/DEPLOYMENT.md` 同步更新实际执行来源与 runbook。
- Gate tests 覆盖 0/5 fallback PASS、1/5 fallback PASS WITH WARNING、2/5 fallback FAIL、连续两次 fallback FAIL、Empty reply HARD FAIL、public non-2xx HARD FAIL，以及 backup/database/immutable-image hard gate 不可绕过。
- Candidate collector 固定执行 5 次真实 HTTPS/认证/RAG/History/metrics/Java/PostgreSQL 路径，为每次请求生成唯一 `X-Request-ID`，并关联保存 Edge、Frontend、Backend 与 Java 日志。

以下 gate 继续保持 HARD，不因 Java degraded policy 放宽：Public HTTPS Empty reply/non-2xx/不完整响应、认证授权与安全边界、数据库与数据一致性、Application Delete 隔离、migration/Alembic、PostgreSQL、backup/restore、rollback assets、6 GiB root capacity、immutable image/revision、artifact parity、required container health/restart/OOM、health/readiness 与持续 runtime error。

Java degraded gate 仅改变已有 local fallback 的统计判断：0/5 为 PASS；恰好 1/5 且 5/5 完整成功、结果正确且不连续为 PASS WITH WARNING；fallback >=2/5、连续两次或 fallback 结果异常为 FAIL。任何 Public Empty reply/non-2xx 仍直接 HARD FAIL。单次正常 latency spike、恢复后的 transient warning 和符合条件的单次 fallback 只记录 WARNING。

## Release identity / immutable artifacts

- Release commit：`38097dd435841f7c4396b168e3777f25a6ff3067`
- Existing annotated tag：`v2.2.0`；未移动、重建或重新创建。
- Backend：`ghcr.io/hkjoker-z/personal-job-agent-backend@sha256:5cd8320c89eb493aa7b9f80b7d892dbddd32fb171d47e9236209729555780648`
- Frontend/Edge：`ghcr.io/hkjoker-z/personal-job-agent-frontend@sha256:e4fb2c7d30a8da1f87b11455d66691f81327e4d9b1ebff807e649b8cf3cc3948`
- Java：`ghcr.io/hkjoker-z/personal-job-agent-java-normalization@sha256:57e3e68c96ca629e4216e4cb19d55c0d9a52ad9bfb2d49c289fdc94f61f0d47f`
- Backend/Frontend OCI revision 均为 release commit，version 均为 `2.2.0`；Candidate 与 Production cutover 使用相同 digest，未 build、retag 或创建 v2.2.1。

## Preflight / backup / restore / rollback

- 切换前 Production：v2.1.0，Alembic `20260820_08`，PostgreSQL 16.9，56 public tables；Redis PONG，Java readiness UP。
- Required containers 全部 healthy、restart=0、OOM=false；health/readiness 与 production runtime regression 通过。
- Root capacity 最低复查值高于 6 GiB hard threshold；Compose exact render 和 immutable image labels/digests 通过。
- Production backup：`/var/backups/personal-job-agent-v2/v2-production/v2-20260822-120843-b1385885`
  - archive SHA-256：`acbe0d7d074ed37186973ed20bc0e4276ea92c8ad220a0de4592838287c688ae`
  - aggregate SHA-256：`f944e552ae8de55a07050e1b90630ced17909ba570e6ff8d72cd34abf8507025`
  - guarded verify、隔离 PostgreSQL 16 restore、strict inventory、Alembic `20260820_08`、56 tables 与 Project Knowledge checksum 全部通过。
- Rollback snapshot：`/var/backups/personal-job-agent-v2/v2.1.0-pre-v2.2.0-20260822T120757Z`
  - aggregate SHA-256：`29c1fb6cc83aa8f5c4932df13027d6f6723b356240f318bd5501ce7d9ec4cd57`
  - v2.1.0 Backend rollback digest：`sha256:984bf21c9130351988f739d15de5cdf47c3d0d41297ace04574c3beafbecb801`
  - v2.1.0 Frontend rollback digest：`sha256:d3de53a7d4bd843b3474c569b7a5c29f418cc102b4767d7c4c83292609204b06`
- 本版本没有新 migration；发布前、cutover 后和 rollback 后 Alembic 都为 `20260820_08`。

## Candidate validation

Candidate 使用独立 Compose project、网络、PostgreSQL/Redis volumes、文件目录与测试账号；RAG、History、metrics、Java、认证和 PostgreSQL persistence 均保持开启。

完整功能 smoke 通过：Login、negative CSRF、Resume、Analyze、History、Project Knowledge/RAG、Applications create/view/duplicate guard、Resume Snapshot、Application physical DELETE；数据库确认目标 Application row count 为 0，同时保留无关 Application、来源 History 与 Resume。

正式 5-run Analyze 结果：

| Run | X-Request-ID | HTTP | E2E ms | Java ms | Fallback |
|---|---|---:|---:|---:|---|
| 1 | `v220-candidate-gate-analyze1-77dffb3eb152` | 200 | 12534.671 | 41.076 | false |
| 2 | `v220-candidate-gate-analyze2-d2831a379b7f` | 200 | 12791.963 | 38.184 | false |
| 3 | `v220-candidate-gate-analyze3-b881ba672bb7` | 200 | 11796.422 | 24.231 | false |
| 4 | `v220-candidate-gate-analyze4-2251fc4f2f5f` | 200 | 9651.299 | 33.083 | false |
| 5 | `v220-candidate-gate-analyze5-cd09d79a6594` | 200 | 12427.799 | 32.643 | false |

5/5 response 完整且正确，History 与 metrics 均持久化，RAG 有命中，四层日志扫描成功，无 relevant errors；fallback 0/5、无连续 fallback。Verdict：**PASS**。Candidate 证据保存后，isolated containers、volumes、network 和数据已清理，临时凭据与私钥已销毁。

## Production cutover / HARD FAIL evidence

- 2026-08-22 12:31:04 UTC 开始 cutover；Worker、Outbox、Backend、Frontend 与 Edge 均切换到既有 v2.2.0 immutable digests。
- Cutover 后 public health/readiness 为 v2.2.0，Alembic `20260820_08`；required containers healthy、restart=0、OOM=false。
- 使用 isolated production account 开始完整功能验收。Login、negative CSRF、Resume create/version/finalize 与 RAG rebuild 成功。
- 首个 production-equivalent Public HTTPS Analyze：
  - X-Request-ID：`v220-production-functional-analyze-94bbfd3d0db0`
  - client：`empty_reply`，HTTP status `null`，response bytes `0`
  - Java：HTTP 200，duration 20 ms，normalization duration 16 ms
  - Backend final status：HTTP 200，timestamp `2026-08-22T12:34:05.681Z`
  - 客户端仍未收到完整 HTTP response，因此按 policy 为 **HARD FAIL**，没有继续正式 5-run Production gate，也没有尝试用正常 Java 响应覆盖 Public failure。
- Edge Nginx 的既有 production config 为 `access_log off`，保存的 Edge Docker log 文件因此为空；Frontend、Backend、Java bounded logs 与连接关闭证据均已保存。当前证据能确认 Backend 完成 200 而 Public connection 无响应，但不能确定 Backend 之后的连接关闭 owner。
- 受保护证据目录：`/var/backups/personal-job-agent-v2/v2.2.0-release-evidence-20260822T120757Z`（mode-protected；不含凭据或响应正文）。

## Rollback / cleanup / final production state

- HARD FAIL 后立即恢复 snapshot 中的 v2.1.0 Compose 与 env，依次恢复 Worker、Outbox、Backend、Frontend、Edge；未执行数据库 downgrade。
- 回滚后的 live Compose/env 与 rollback snapshot 逐字一致。
- 当前 Production：v2.1.0；Alembic `20260820_08`；PostgreSQL、Redis、Java 正常。
- Required Production/Java containers 全部 healthy、restart=0、OOM=false；public HTTPS health/readiness 通过。
- Isolated production account 及其 Session、Resume、History、Analyze idempotency 与 Audit rows 已事务性精确删除；复查 user、audit、history、resume 均为 0。第一次 cleanup 尝试因 `resume_versions.created_by` FK 被 PostgreSQL 拒绝且完整 rollback，随后先删除隔离 Resume hierarchy 再删除 user，最终通过，没有触及真实生产用户数据。
- Acceptance 与 cutover 临时凭据、candidate 私钥及复制的 production env secrets 已安全销毁。

## Known warnings / follow-up

- Candidate 的外部测试端口不经过云防火墙公开；正式 Candidate gate 使用本机 HTTPS Edge-equivalent path、独立短期证书，并完整经过 Edge → Frontend Nginx → Backend。该路径的 5-run gate 成功。
- 现有 candidate 测试证书已过期，本次使用隔离的短期自签名证书；没有更改 Production TLS。
- 首次 backup invocation 因未显式提供 immutable Postgres tool/server image 而被脚本安全拒绝；修正参数后完整 backup/verify/restore 全部通过。首次 restore owner-map 演练少列 `pja_bootstrap`，严格 inventory 正确拒绝两个 owner mismatches；补齐三项窄映射后通过。
- Production Empty reply 是本版本重新发布的最终 blocker。下一次尝试不得将其降级为 warning；应先补齐 Edge/Frontend access 或 connection-level observability，在不改变业务逻辑、Java timeout 或安全 hard gates 的前提下定位 Backend 200 之后的连接关闭位置。

本次没有创建 `2026-08-22-v2.2.0-release-work-report.md`，因为 v2.2.0 未成功留在 Production。
