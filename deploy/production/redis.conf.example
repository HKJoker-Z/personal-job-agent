bind 0.0.0.0
protected-mode yes
port 6379
appendonly yes
appendfsync everysec
dir /data
maxmemory 256mb
maxmemory-policy noeviction
# Add a generated `requirepass` value only in the host-managed production copy.
# Never commit that credential.
